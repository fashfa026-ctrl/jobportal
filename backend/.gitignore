# Environment variables (NEVER commit these — contains secrets)
.env
.env.local
.env.*.local

# Dependencies
node_modules

# Uploaded user files (resumes, avatars, logos) — should not be in version control
uploads/*
!uploads/.gitkeep

# Logs
logs
*.log
npm-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?